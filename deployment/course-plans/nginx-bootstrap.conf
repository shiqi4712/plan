server {
    listen 80;
    server_name plan.bcmty.cn;

    location ^~ /.well-known/acme-challenge/ {
        root /srv/course-plans/acme;
        default_type text/plain;
    }

    location / { return 404; }
}
